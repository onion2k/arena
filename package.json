{
  "name": "arena",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "calibrate": "vite-node scripts/calibrate.ts"
  },
  "dependencies": {
    "artshape-render": "github:onion2k/artshape-render#v0.11.0"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.44",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^3.2.7"
  }
}
